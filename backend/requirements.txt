# Web layer on top of the root project's src/ (pandas, numpy, fyers-apiv3, pyotp, truststore,
# setuptools<81 — see ../requirements.txt, install that too).
fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pyjwt==2.10.1
cryptography==44.0.0
itsdangerous==2.2.0
python-dotenv==1.0.1
pytest-asyncio==0.24.0
openpyxl==3.1.5
# httpx is already pulled in transitively (needed for FastAPI's TestClient) — not pinned here
# to avoid conflicting with whatever version python-telegram-bot/fyers-apiv3 already constrained.

